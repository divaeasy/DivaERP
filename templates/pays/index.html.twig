{% extends 'template_oukatech.html.twig' %}

{% block title %}Pays{% endblock %}

{% block body %}

<br/>
<div class="input-group"  >

<a class="btn btn-primary" href="{{path('pays.edit')}}" role="button">Ajouter</a>


</div>

<br/>
<div class="row">

  <table class="table table-striped">
    <thead class="thead-light">
      <tr>
        <th scope="col">Id</th>
        <th scope="col">Libellé</th>
        
   
        <th scope="col"></th>
  
      </tr>
    </thead>
    <tbody>
      {% for pays in pays %}
      <tr>
        <th scope="row"><a href="{{ path('pays.edit',{id: pays.id}) }}" >{{pays.id}}</a></th>
        <th scope="row">{{pays.libelle}}</th>
  

        
        {%if is_granted('ROLE_ADMIN')%}
        <td><a href="{{ path('pays.edit',{id: pays.id}) }}" ><i class="fa-solid fa-user-pen"></i></a></td>
        {% endif %}
      </tr>
      {% endfor %}
    </tbody>
  </table>

</div>
{% endblock %}

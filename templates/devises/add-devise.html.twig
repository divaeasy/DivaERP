{% extends "template_oukatech.html.twig" %}
{% block PageTitle%}
    Devise
{% endblock %}
{% block title %}Devise{% endblock %}
{% block body %}
{{ form_start(devise)}}
    <div class="row">
        <div class="col">{{form_row(devise.code)}}</div>
        <div class="col">{{form_row(devise.libelle)}}</div>
    </div>
    
    
    <button type="submit" class="btn btn-lg btn-primary mt-2">Enregistrer</button>
{{form_rest(devise)}}
{% endblock %}
{% block javascripts %}
    <script>
        $(document).ready(function() {
            $('.js-example-basic-single').select2();
        });
    </script>
{% endblock %}